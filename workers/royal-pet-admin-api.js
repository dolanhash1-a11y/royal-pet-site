// Cloudflare Worker + D1 backend for the Royal Pet cabinet.
// Bind D1 as DB. Set ADMIN_USERNAME, ADMIN_PASSWORD_HASH and SESSION_SECRET as Worker secrets/vars.
const headers=req=>({'Access-Control-Allow-Origin':req.headers.get('Origin')||'','Access-Control-Allow-Credentials':'true','Access-Control-Allow-Headers':'Content-Type,Authorization','Access-Control-Allow-Methods':'GET,POST,PATCH,PUT,OPTIONS','Vary':'Origin'});
const json=(req,x,status=200,extra={})=>new Response(JSON.stringify(x),{status,headers:{...headers(req),...extra,'Content-Type':'application/json'}});
async function hash(password,salt,iterations=120000){const key=await crypto.subtle.importKey('raw',new TextEncoder().encode(password),'PBKDF2',false,['deriveBits']);const bits=await crypto.subtle.deriveBits({name:'PBKDF2',salt,iterations,hash:'SHA-256'},key,256);return new Uint8Array(bits)}
const hex=a=>[...a].map(x=>x.toString(16).padStart(2,'0')).join('');
function b64(a){let s='';for(const x of a)s+=String.fromCharCode(x);return btoa(s).replace(/=+$/,'').replace(/\+/g,'-').replace(/\//g,'_')}
function unb64(s){s=s.replace(/-/g,'+').replace(/_/g,'/');while(s.length%4)s+='=';const bin=atob(s);return Uint8Array.from(bin,c=>c.charCodeAt(0))}
async function token(username,secret){const payload=btoa(JSON.stringify({u:username,e:Date.now()+86400000}));const key=await crypto.subtle.importKey('raw',new TextEncoder().encode(username+':'+secret),{name:'HMAC',hash:'SHA-256'},false,['sign']);const sig=new Uint8Array(await crypto.subtle.sign('HMAC',key,new TextEncoder().encode(payload)));return payload+'.'+b64(sig)}
async function valid(req,secret){let h=req.headers.get('Authorization')||'';if(!h.startsWith('Bearer ')){const c=req.headers.get('Cookie')||'';const m=c.match(/(?:^|;\s*)rp_session=([^;]+)/);if(m)h='Bearer '+m[1]}if(!h.startsWith('Bearer '))return false;try{const [p,s]=h.slice(7).split('.');const d=JSON.parse(atob(p));if(!d.e||d.e<Date.now())return false;const key=await crypto.subtle.importKey('raw',new TextEncoder().encode(d.u+':'+secret),{name:'HMAC',hash:'SHA-256'},false,['verify']);return crypto.subtle.verify('HMAC',key,unb64(s),new TextEncoder().encode(p))}catch{return false}}
async function body(req){try{return await req.json()}catch{return{}}}
export default{async fetch(req,env){if(req.method==='OPTIONS')return new Response(null,{headers:headers(req)});const u=new URL(req.url);try{
if(u.pathname==='/auth/login'&&req.method==='POST'){const b=await body(req);const [saltHex,it,hashHex]=(env.ADMIN_PASSWORD_HASH||'').split(':');if(!b.username||!b.password||b.username!==env.ADMIN_USERNAME||!saltHex||!hashHex)return json(req,{ok:false},401);const salt=new Uint8Array(saltHex.match(/.{2}/g).map(x=>parseInt(x,16)));const out=await hash(b.password,salt,Number(it)||120000);if(hex(out)!==hashHex)return json(req,{ok:false},401);const t=await token(b.username,env.SESSION_SECRET);return json(req,{ok:true},200,{'Set-Cookie':`rp_session=${t}; HttpOnly; Secure; SameSite=None; Path=/; Max-Age=86400`})}
if(u.pathname==='/appointments'&&req.method==='POST'){const b=await body(req);if(!b.pet_name||!b.owner_contact)return json(req,{success:false,error:'missing_fields'},400);const id=crypto.randomUUID();await env.DB.prepare(`INSERT INTO appointments(id,pet_name,age,breed,owner_contact,last_grooming,preferred_time,additional_services,home_care,comment,status,created_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?)`).bind(id,b.pet_name||'',b.age||'',b.breed||'',b.owner_contact||'',b.last_grooming||'',b.preferred_time||'',b.additional_services||'',b.home_care||'',b.comment||'','new',new Date().toISOString()).run();return json(req,{success:true,id})}
if(!(await valid(req,env.SESSION_SECRET)))return json(req,{error:'unauthorized'},401);
if(u.pathname==='/appointments'&&req.method==='GET'){const r=await env.DB.prepare('SELECT * FROM appointments ORDER BY preferred_time ASC, created_at DESC').all();return json(req,r.results||[])}
if(u.pathname.startsWith('/appointments/')&&req.method==='PATCH'){const id=decodeURIComponent(u.pathname.split('/').pop());const b=await body(req);await env.DB.prepare('UPDATE appointments SET status=? WHERE id=?').bind(b.status||'new',id).run();return json(req,{ok:true})}
if(u.pathname==='/hours'&&req.method==='GET'){const r=await env.DB.prepare('SELECT day,value FROM hours ORDER BY day').all();return json(req,Object.fromEntries((r.results||[]).map(x=>[x.day,x.value])))}
if(u.pathname==='/hours'&&req.method==='PUT'){const b=await body(req);for(const [day,value] of Object.entries(b))await env.DB.prepare('INSERT INTO hours(day,value) VALUES(?,?) ON CONFLICT(day) DO UPDATE SET value=excluded.value').bind(day,String(value)).run();return json(req,{ok:true})}
if(u.pathname==='/reviews'&&req.method==='GET'){const r=await env.DB.prepare('SELECT * FROM reviews ORDER BY created_at DESC').all();return json(req,r.results||[])}
if(u.pathname.startsWith('/reviews/')&&req.method==='PATCH'){const id=decodeURIComponent(u.pathname.split('/').pop());const b=await body(req);await env.DB.prepare('UPDATE reviews SET status=COALESCE(?,status),reply=COALESCE(?,reply) WHERE id=?').bind(b.status??null,b.reply??null,id).run();return json(req,{ok:true})}
return json(req,{error:'not_found'},404)}catch(e){return json(req,{error:String(e.message||e)},500)}}};
