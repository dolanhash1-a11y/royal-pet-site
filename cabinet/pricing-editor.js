(()=>{
const API=(window.ROYAL_PET_ADMIN_API||'').replace(/\/$/,'');
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const money=v=>Number(v||0).toLocaleString('uk-UA');
const originalFetch=window.fetch.bind(window);
const numericPrice=v=>{const n=parseFloat(String(v??'').replace(',','.').replace(/[^0-9.\-]/g,''));return Number.isFinite(n)?n:0};
async function getServices(){const r=await originalFetch(API+'/services',{credentials:'include'});if(!r.ok)throw Error('Не вдалося завантажити прайс');return r.json()}
async function getSaved(){try{const r=await originalFetch(API+'/hours',{credentials:'include'});if(r.ok){const h=await r.json();if(h.__pricing__)return JSON.parse(h.__pricing__)||{services:{}}}}catch{}return{services:{}}}
function style(){if(document.getElementById('rp-price-editor-style'))return;const s=document.createElement('style');s.id='rp-price-editor-style';s.textContent=`#view-pricing .price-edit-grid{display:grid;gap:10px}#view-pricing .price-row{display:grid;grid-template-columns:minmax(220px,1fr) auto auto;gap:14px;align-items:center;padding:15px 16px;border:1px solid rgba(215,173,85,.14);border-radius:12px;background:rgba(255,255,255,.02)}#view-pricing .price-label{font-weight:700;font-size:15px}#view-pricing .price-base{font-weight:700;white-space:nowrap}#view-pricing .price-surcharge{font-size:13px;color:#d7ad55;white-space:nowrap}#view-pricing .price-note{font-size:12px;color:#999;margin-top:4px;line-height:1.45}@media(max-width:700px){#view-pricing .price-row{grid-template-columns:1fr auto;gap:8px}.price-row .price-surcharge{grid-column:1/-1}.price-row .price-base{grid-column:2;grid-row:1}}`;document.head.appendChild(s)}
async function render(){style();document.querySelectorAll('.view').forEach(v=>v.classList.add('hidden'));let root=document.getElementById('view-pricing');if(!root){root=document.createElement('div');root.id='view-pricing';root.className='view';document.querySelector('.main')?.appendChild(root)}root.classList.remove('hidden');document.querySelectorAll('.nav-item').forEach(v=>v.classList.toggle('active',v.dataset.view==='pricing'));const title=document.getElementById('page-title');if(title)title.textContent='Прайс';root.innerHTML='<div class="card">Завантаження прайсу…</div>';try{const [services,saved]=await Promise.all([getServices(),getSaved()]);const p=saved.services||{};const rows=services.filter(s=>s.active!==false).map(s=>{const sp=p[s.id]||{};const base=numericPrice(s.price);const light=numericPrice(sp.light);const severe=numericPrice(sp.severe);const surcharges=[];if(light>0)surcharges.push(`Ковтуни +${money(light)} грн`);if(severe>0)surcharges.push(`Сильні ковтуни +${money(severe)} грн`);return `<div class="price-row"><div><div class="price-label">${esc(s.title)}</div>${surcharges.length?`<div class="price-surcharge">${surcharges.map(esc).join(' · ')}</div>`:''}</div><div class="price-base">${money(base)} грн</div></div>`}).join('');root.innerHTML=`<div class="toolbar"><div><h3>Прайс</h3><span style="color:#aaa">Готові послуги: назва, базова ціна та лише вказані доплати.</span></div><button class="small-btn" id="price-reload">Оновити</button></div><div class="card"><div class="price-edit-grid">${rows||'<div>Послуг поки немає.</div>'}</div><div class="price-note" style="margin-top:14px"><b>Доплати:</b> показуються тільки якщо для послуги вказана сума за ковтуни або сильні ковтуни.</div></div>`;root.querySelector('#price-reload').onclick=render}catch(e){root.innerHTML=`<div class="card error">Помилка: ${esc(e.message)}</div>`}}
function install(){const button=document.querySelector('[data-view="pricing"]');if(!button)return;if(button.dataset.priceEditorInstalled==='1')return;button.dataset.priceEditorInstalled='1';button.onclick=render}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
new MutationObserver(install).observe(document.body,{childList:true,subtree:true});
})();