(()=>{
const API=(window.ROYAL_PET_ADMIN_API||'').replace(/\/$/,'');
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const money=v=>Number(v||0).toLocaleString('uk-UA');
const coatLabel=v=>({none:'Нормальний стан',light:'Є ковтуни',severe:'Сильні ковтуни / запущена шерсть'})[v]||'Нормальний стан';
const statusLabel=v=>({new:'Нова',confirmed:'Підтверджена',done:'Виконана',cancelled:'Скасована',deleted:'Видалена'})[v]||v||'—';
async function getAppointments(){const r=await fetch(API+'/appointments',{credentials:'include',cache:'no-store'});if(!r.ok)throw Error('Не вдалося отримати записи');const data=await r.json();return Array.isArray(data)?data:(Array.isArray(data?.results)?data.results:[])}
function parseMeta(x){try{const o=JSON.parse(x?.additional_services||'');if(o&&typeof o==='object'&&!Array.isArray(o))return o}catch{}return{}}
function serviceNames(m){return Array.isArray(m.services)?m.services.map(v=>typeof v==='string'?v:v?.title).filter(Boolean):[]}
function setActive(){document.querySelectorAll('.nav-item').forEach(x=>x.classList.toggle('active',x.dataset.view==='calendar'));const t=document.getElementById('page-title');if(t)t.textContent='Календар'}
function dateOf(v){const d=new Date(String(v||'').replace(' ','T'));return isNaN(d)?null:d}
function dayKey(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
function dayLabel(d){return d.toLocaleDateString('uk-UA',{weekday:'long',day:'numeric',month:'long',year:'numeric'})}
function card(x){const m=parseMeta(x),svc=serviceNames(m),price=m.final_price??m.estimated_total??'',d=dateOf(x.preferred_time);if(!d)return '';const tm=d.toLocaleTimeString('uk-UA',{hour:'2-digit',minute:'2-digit'});return `<div class="card calendar-item"><div class="calendar-time">${esc(tm)}</div><div><b>${esc(x.pet_name||'—')}</b><span>${esc(x.breed||'Без породи')}</span><span>${m.animal_type==='cat'?'Кіт':'Собака'}${m.breed_category?` · ${esc(m.breed_category)}`:''}</span></div><div><b>${esc(x.owner_name||'—')}</b><span>${esc(x.owner_contact||'')}</span></div><div><span>${svc.length?esc(svc.join(', ')):'Послуги не вказані'}</span><span>Стан шерсті: ${esc(coatLabel(m.coat_condition))}</span>${price!==''?`<span>${esc(m.final_price!=null?'До оплати: '+money(price)+' грн':'Орієнтовно: '+money(price)+' грн')}</span>`:''}</div><div><span class="badge ${esc(x.status||'new')}">${esc(statusLabel(x.status))}</span><button class="small-btn gold" type="button" data-cal-edit="${esc(x.id)}">Відкрити</button></div></div>`}
async function renderCalendar(){const root=document.getElementById('view-calendar');if(!root)return;document.querySelectorAll('.view').forEach(v=>v.classList.add('hidden'));root.classList.remove('hidden');setActive();root.innerHTML='<div class="card">Завантаження календаря…</div>';try{const all=await getAppointments();const records=all.filter(x=>!['cancelled','deleted'].includes(String(x.status||'').toLowerCase())&&dateOf(x.preferred_time));records.sort((a,b)=>dateOf(a.preferred_time)-dateOf(b.preferred_time));const groups=new Map();records.forEach(x=>{const d=dateOf(x.preferred_time),key=dayKey(d);if(!groups.has(key))groups.set(key,{date:d,items:[]});groups.get(key).items.push(x)});const html=[...groups.values()].map(g=>`<section class="calendar-day"><h3>${esc(dayLabel(g.date))}</h3><div class="calendar-list">${g.items.map(card).join('')}</div></section>`).join('');root.innerHTML=`<div class="toolbar"><div><h3>Записи в календарі</h3><span style="color:#aaa">Всього записів: ${records.length}</span></div><button class="small-btn" type="button" id="calendar-refresh">Оновити</button></div>${html||'<div class="card">Записів у календарі поки немає.</div>'}`;root.querySelector('#calendar-refresh')?.addEventListener('click',renderCalendar);root.querySelectorAll('[data-cal-edit]').forEach(btn=>btn.addEventListener('click',()=>{const x=records.find(v=>v.id===btn.dataset.calEdit);if(x){if(typeof window.showAppointment==='function')window.showAppointment(x);else if(typeof window.royalPetOpenAppointment==='function')window.royalPetOpenAppointment(x)}}))}catch(e){root.innerHTML=`<div class="card error">Помилка завантаження календаря: ${esc(e.message)}</div>`}}
function hook(){document.addEventListener('click',e=>{const btn=e.target.closest('.sidebar .nav-item[data-view="calendar"], [data-mobile-view="calendar"]');if(!btn)return;e.preventDefault();e.stopImmediatePropagation();renderCalendar()},true)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',hook,{once:true});else hook();window.royalPetCalendar=renderCalendar;
})();