(()=>{const A=(window.ROYAL_PET_ADMIN_API||'').replace(/\/$/,'');const days=[['monday','Понеділок'],['tuesday','Вівторок'],['wednesday','Середа'],['thursday','Четвер'],['friday','П’ятниця'],['saturday','Субота'],['sunday','Неділя']];const root=()=>document.getElementById('profile-hours');const css=`<style>.profile-hours{margin-top:18px}.profile-hours-card{border:1px solid rgba(215,173,85,.28);border-radius:16px;padding:15px;background:#101010}.profile-hours-head{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-bottom:10px}.profile-hours-head h3{margin:0}.profile-hours-refresh{width:auto!important;margin:0!important;padding:8px 11px!important;font-size:13px!important}.profile-hours-list{display:grid;gap:7px}.profile-hours-row{display:flex;justify-content:space-between;gap:12px;padding:9px 0;border-top:1px solid rgba(255,255,255,.07)}.profile-hours-row:first-child{border-top:0}.profile-hours-row.today{color:#d7ad55}.profile-hours-row span:last-child{font-weight:700;text-align:right}.profile-hours-row.closed span:last-child{color:#ff9a9a}.profile-hours-note{margin-top:10px;color:#777;font-size:12px}</style>`;async function load(){const r=root();if(!r)return;r.innerHTML='<div class="profile-hours-card"><div class="muted">Завантажуємо графік…</div></div>';try{const res=await fetch(`${A}/public/hours?sync=${Date.now()}`,{cache:'no-store'});if(!res.ok)throw Error('Не вдалося завантажити графік');const h=await res.json();const jsDay=new Date().getDay();const currentKey=days[(jsDay+6)%7][0];r.innerHTML=css+`<div class="profile-hours-card"><div class="profile-hours-head"><h3>Графік роботи</h3><button type="button" class="ghost profile-hours-refresh">Оновити</button></div><div class="profile-hours-list">${days.map(([k,l])=>{const v=String(h?.[k]??'').trim();const closed=/вихідний|закрит/i.test(v);return `<div class="profile-hours-row ${k===currentKey?'today ':''}${closed?'closed':''}"><span>${k===currentKey?'● ':''}${l}</span><span>${closed?'Вихідний':(v||'Не задано')}</span></div>`}).join('')}</div><div class="profile-hours-note">Розклад оновлюється з адмінпанелі Royal Pet.</div></div>`;r.querySelector('.profile-hours-refresh').onclick=load}catch(e){r.innerHTML=`<div class="profile-hours-card"><div class="muted">⚠️ ${e.message}</div><button type="button" class="ghost profile-hours-refresh">Спробувати ще раз</button></div>`;r.querySelector('.profile-hours-refresh').onclick=load}}window.renderCustomerSchedule=load;document.addEventListener('DOMContentLoaded',load);})();
